<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js" integrity="sha512-v2CJ7UaYy4JwqLDIrZUI/4hqeoQieOmAZNXBeQyjo21dadnwR+8ZaIJVT8EE2iyI61OV8e6M8PP2/4hpQINQ/g==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
        {% block stylesheets %}
        {% endblock %}

        {% block javascriptInitial %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            {% block javascripts %}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <div class="container-fluid px-0">
            <header class="d-flex justify-content-center py-3 text-bg-dark fixed-top w-100" style="padding-top: 5px !important; padding-bottom: 5px !important; z-index: 1030;">
            <ul class="nav nav-pills">
                <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-white text-decoration-none">
            <svg class="bi pe-none me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
            <span class="fs-4">ERPCRM</span>
            </a>
            <li class="nav-item me-3">
                <a href="{{ path('app_index') }}" class="nav-link link-white {% if app.request.attributes.get('_route') == 'app_index' or app.request.uri matches "/user/" %}active{% endif %} d-flex flex-column align-items-center" aria-current="page">
                <i class="fa-solid fa-house"></i>
                <span>Accueil</span>
                </a>
            </li>
            <li class="nav-item me-3">
                <a href="#" class="nav-link link-white d-flex flex-column align-items-center">
                <i class="fa-solid fa-users"></i>
                <span>Tiers</span>
                </a>
            </li>
            <li class="nav-item me-3">
                <a href="{{ path('app_services_index') }}" class="nav-link link-white {% if app.request.uri matches "/services/" %}active{% endif %} d-flex flex-column align-items-center">
                <i class="fa-solid fa-cogs"></i>
                <span>Services</span>
                </a>
            </li>
            <li class="nav-item me-3">
                <a href="#" class="nav-link link-white  d-flex flex-column align-items-center">
                <i class="fa-solid fa-store"></i>
                <span>Commerce</span>
                </a>
            </li>
            <li class="nav-item me-3">
                <a href="#" class="nav-link link-white d-flex flex-column align-items-center">
                <i class="fa-solid fa-file-invoice-dollar"></i>
                <span>Facturation</span>
                </a>
            </li>
            <li class="nav-item me-3">
                <a href="#" class="nav-link link-white d-flex flex-column align-items-center">
                <i class="fa-solid fa-tools"></i>
                <span>Outils</span>
                </a>
            </li>
            </ul>
            <div class="dropdown ms-auto d-flex align-items-center">
            <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                <img src="https://github.com/mdo.png" alt="" width="32" height="32" class="rounded-circle me-2">
                <strong>{{ app.user.firstname|capitalize }} {{ app.user.name|capitalize }}</strong>
            </a>
            <ul class="dropdown-menu dropdown-menu-dark text-small shadow">
                <li><a class="dropdown-item" href="#">New project...</a></li>
                <li><a class="dropdown-item" href="#">Settings</a></li>
                <li><a class="dropdown-item" href="#">Profile</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="#">Sign out</a></li>
            </ul>
            </div>
            </header>
        </div>
        <div class="d-flex" style="margin-top: 60px;">
            <div class="d-flex flex-column flex-shrink-0 p-3 text-bg-dark vh-100" style="position: fixed;">
            {% include 'panel/_lateral_panel.html.twig' %}
            </div>
            <div style="padding:10px;margin-left:300px;" class="flex-grow-1">
            {% for label, messages in app.flashes %}
                <div class="alert alert-{{ label }}">
                    {% for message in messages %}
                        <p>{{ message }}</p>
                    {% endfor %}
                </div>
            {% endfor %}
            {% block body %}
            {% endblock %}
            </div>
        </div>
    </body>
</html>
